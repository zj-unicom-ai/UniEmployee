# UniEmployee 领域上下文

本文档统一数字员工平台的业务术语与责任边界，不替代接口、表结构或部署文档。

## 身份与授权

- **租户（Tenant）**：企业数据与配置资产的绝对隔离边界；一期单企业部署中仍以固定 `tenant_id` 表示该边界。
- **组织（Organization）**：租户内的部门树，用于表达归属和组织范围，不能替代租户隔离。
- **主体（Principal）**：发起请求的用户或服务账号；认证服务是其身份、状态和租户归属的唯一事实来源。
- **用户组（User Group）**：租户内的授权主体集合，可由身份源组同步；它不是组织部门。
- **角色（Role）**：一组稳定权限的命名职责，例如平台管理员、资源管理员或普通员工。
- **权限（Permission）**：对资源类型执行一个动作的原子许可，例如使用数字员工、读取知识库或调用连接器。
- **数据范围（Data Scope）**：权限可作用的数据集合，只能是本人、指定用户、组织树或全租户之一。
- **身份源映射（Identity Mapping）**：将 IdP 的外部组映射到本平台用户组或角色的受控规则；外部 role 声明本身不是平台授权。

## 数字员工资源

- **数字员工（Employee）**：可复用的岗位能力模板，由人设、模型和资源装配而成。
- **资源（Resource）**：可被员工使用或运行时读取的受控资产，包括知识库、数据源和连接器。
- **可见范围（Visibility）**：资源可被哪些租户内主体发现和使用的策略；它与员工装配关系共同决定实际可用性。
- **员工分配（Assignment）**：把一个可见的数字员工授予一个主体的关系；撤销后必须阻止新运行和审批恢复。
- **授权预览（Authorization Preview）**：在不执行模型或工具的前提下，计算某主体当前可使用的员工、知识库、数据源与连接器的解释性结果。

## 关系约束

```text
租户 -> 组织 / 主体 / 用户组 / 资源
用户组 + 角色 -> 权限 + 数据范围
IdP 外部组 -> 身份源映射 -> 用户组 / 角色
资源可见范围 + 员工装配 + 员工分配 -> 主体有效能力
```

## 当前产品口径

- 普通用户不能通过覆盖配置扩大员工、知识库、数据源或连接器的可见范围。
- 未显式允许的资源默认不可用；平台公共资源必须被明确标记。
- 授权预览必须与运行时使用同一套判定规则，不能只做管理端展示。
