# Security Policy

## 支持的版本

| 版本 | 支持状态 |
|------|----------|
| 0.3.x（latest） | ✅ 积极维护 |
| 更早版本 | ❌ 不受支持 |

## 报告安全问题

**请勿在公开 Issue 中提交安全漏洞**。请将漏洞详情发送到维护团队邮箱：

- 邮箱：`<INSERT_MAINTAINER_EMAIL>`
- 主题请注明 `[UniEmployee Security]` 前缀

请包含以下信息以便复现：

- 受影响的版本 / 提交号
- 漏洞类型（如 SQL 注入、XSS、越权访问、密钥泄露等）
- 复现步骤与最小复现样例
- 影响的接口 / 模块

## 处理流程

1. 维护者确认收到报告（48 小时内）
2. 评估影响范围与严重级别
3. 修复并发布补丁版本，同时在 CHANGELOG 中注明
4. 修复发布后再公开披露（协调披露）

## 安全基线提醒

部署时请务必：

- 修改 `.env` 中的 `JWT_SECRET`（长随机串），更换后所有已签发 token 立即失效
- 修改默认管理员密码 `admin/admin123`（首登强制改密）
- 生产环境设置 `LOG_LEVEL` 并关注 `/health` 探针与访问日志
- 不将 `.env`、SQLite 数据库、日志文件提交到公开仓库
